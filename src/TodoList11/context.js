import React, { Component } from 'react';
import PropTypes from 'prop-types'

// 高阶组件  其实是一个函数，传入一个组件，返回一个新组件
 
class HomePage extends Component { 
	
	render() {
		return (
			<div>
				高阶组件
			</div>
		)
	}
}

export default HomePage;
   